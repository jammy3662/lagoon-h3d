#include "disk.h"

#include <stdio.h>

// TODO: replace with io struct
typedef void* Pack;

// currently loaded resource pack,
// can be null (load from disk)
Pack* res;

void loadPack (const char* filepath)
{
	
}